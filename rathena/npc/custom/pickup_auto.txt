-	script	Pickup_Auto_System	-1,{
OnPCLoginEvent:
	// Esperamos un segundo para que el personaje termine de cargar
	sleep2 500;

	// Forzamos la entrega de la habilidad en cada login.
	// Al no usar el "if", nos aseguramos de que siempre la tengas al entrar,
	// independientemente de si el servidor la borró al salir.
	skill 11000,1,0;
	
	
	end;
}